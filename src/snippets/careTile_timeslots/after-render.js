careTile_timeslots: function() {
    // We can't put templates inside templates, so this fn moves them to end of the body. Also removes duplicate templates (by identical ID)
    // wompLib.moveAmpListTemplates('#timeSlotsTemplate')
}
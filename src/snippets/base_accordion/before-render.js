base_accordion: async function(dataStr) {
  wompLib.renderSnippetProps('base_accordion', dataStr)
  // TODO - make it work with newer wRender fn
  // wompLib.passSnippetProps("base_accordion", dataStr)
}
base_modal: function() {
  
  // !! NOTE - Need a better solution for this. Causes page to jump to top.
  // Add the modal-open lock to specific pages.
  // Can't be added to every page because the `!impoertant` is amp-invalid
  // if (wData.isProviders) {
  //   const styleAmpCustom = $('style[amp-custom]')
  //   styleAmpCustom.html(
  //     styleAmpCustom.html() + `
  //     .modal-open-lock.modal-open-lock {
  //       height: 100vh !important;
  //       overflow-y: hidden !important;
  //     }`
  //   )
  // }
}